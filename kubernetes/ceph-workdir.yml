apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: katerndahl
  name: workdir-volume
spec:
  storageClassName: cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 400G
