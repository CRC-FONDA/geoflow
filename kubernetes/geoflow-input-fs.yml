apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: katerndahl
  name: geoflow-input
spec:
  storageClassName: cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 200G
