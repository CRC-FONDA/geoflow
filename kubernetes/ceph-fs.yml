apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: katerndahl
  name: ceph-volume-geoflow
spec:
  storageClassName: cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 400G
