process {
    container = 'floriankaterndahl/geoflow:0.0.1'
}

docker {
    enabled = true
    remove = true
    temp = 'auto'
    fixOwnership = true
    runOptions = '--cpus=8 --volume /data/Dagobah/dc/deu/ard:/data/ard'
}